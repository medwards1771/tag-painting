<div class="row">

  <p>
  <img class="painting" title="<%= @painting.title %>" src="<%= @painting.url %>" alt="<%= @painting.description %>">
  </p>
  <%= render 'tags/form', painting_id: @painting.id %>


  <ul class="tags-container">
    <% @tags.each do |tag| %>
      <%= render 'tags/tag', {tag: tag} %>
    <% end %>
  <ul>

</div>

